module test_mod;

use arduino;

include model;

default flow acquire(){
	const int size = 10;
	int ring[size] rbuf;
	int[size] buf, ybuf, zbuf;

	pinPeripheral(3,4);

	mpu.getFIFOBytes(buf, size);
	ybuf = modelData * buf;

	zbuf = modelData * ybuf;

	next(zbuf);
}

