module test_mod;

use arduino;

include model;

default flow acquire(){
	const int size = 10;
	int ring[size] buf;
	int[size] ybuf;

	pinPeripheral(3,4);

	buf.push(3);
	mpu.getFIFOBytes(buf, size);

	ybuf = modelData * buf + modelData * buf;
	next(ybuf);
}

