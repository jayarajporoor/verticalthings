module test_mod2;

use arduino;

int[10] fff;

void compute(int[10] data, int offset){
	for i in [0,10] {
	    fff[i] = data[i] + offset;
	}
}

default flow process(int[10] val){
    const int x = 3;
    pinPeripheral(3,4);
    compute(val, x);
}