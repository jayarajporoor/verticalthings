module test_mod;
use arduino;

MPU mpu;

default flow test_func(){
    int sum, size;
    pinPeripheral(3,false);
	mpu.getFIFOBytes(buf, size);
    for i in [0,20]{
        temp();
    }
}

void temp(){
    int sum1;
    uint8_t[10] A;
    int[20] B;
    int C;
    B = 0;
    C= 0;
    A = 0;
    C = (B + A)*(B+A);
}