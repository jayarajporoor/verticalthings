module test_mod;
use arduino;
float[30] buf;

MPU mpu;

default flow test_func(){
    int sum, size;
    pinPeripheral(3,4);
	mpu.getFIFOBytes(buf, size);
    for i in [0,20]{
        temp();
    }
}

void temp(){
    int sum1;
    int[20][20] A;
    int[20][20] B;
    int[20][20] C;
    B = 0;
    C= 0;
    C = A*B;
}